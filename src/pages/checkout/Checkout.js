import React from 'react';
import styles from './CheckoutDetails.module.scss';

const Checkout = () => {
    return (
        <div>
            <h2>Checkout</h2>
        </div>
    );
};

export default Checkout;